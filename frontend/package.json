{
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --port 3001",
    "start": "react-scripts start",
    "serve": "serve dist -l 3001 -s",
    "host": "git pull && npm run build && npm run serve"
  },
  "dependencies": {
    "express": "^4.18.2",
    "outlet": "^0.1.5",
    "react": "^18.2.0",
    "react-router-dom": "^6.18.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@types/react": "^18.0.17",
    "@types/react-dom": "^18.0.6",
    "@vitejs/plugin-react": "^2.1.0",
    "vite": "^3.2.7"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
